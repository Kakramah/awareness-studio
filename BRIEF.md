# بريف استوديو التوعية: للتعديل في أي مساعد ذكي أو محادثة جديدة

## ١. ما هو

أداة ويب عربية (RTL) تعمل كلها في المتصفح، بلا خادم ولا مكتبات ولا أداة بناء. يصنع بها خلدون عكرمة منشورات السوشيال ميديا وبوستراتها.

- **الرابط الحيّ:** https://kakramah.github.io/awareness-studio/
- **المستودع:** https://github.com/Kakramah/awareness-studio (عام، الفرع `main`، ينشره GitHub Pages من الجذر)
- **النسخة المحلية عند خلدون:** `~/khaldoun-library/Projects/awareness-studio/`

## ٢. ما يفعله

- **محرّر بطبقات:** كل عنصر (عنوان، سطر داعم، شعار، وسم، حساب، عقاب، صورة، أشكال، نصوص حرّة) يُنقر على اللوحة، فيُسحب ويُكبَّر ويُضبط. وفيه مغناطيس ومحاذاة وتجميع ونسخ ولصق وتراجع.
- **الصورة:**
  - تُفتح نافذة القصّ عند الرفع.
  - القصّ حرّ أو بنسبة، مع تدوير وتقويم وقلب.
  - «ملء» أو «احتواء كامل» للصور التي فيها كتابة.
  - ضبط الضوء واللون والتفاصيل، وتسعة فلاتر جاهزة.
  - ستة مواضع للصورة في التصميم.
- **المقاسات:** سبعة (4:5 · 1:1 · 3:4 · 9:16 · 16:9 · 1.91:1 · 2:3)، ومقاس حرّ، و«بمقاس الصورة».
- **ثلاث هويات:** سوريا الجديدة · خلدون عكرمة · حرّ.
- **مولّد أفكار:** 16 موضوعاً × 8 أساليب بلاغية، و90 فكرة جاهزة، وبرومبت صورة لكل فكرة.
- **التصدير:** PNG أو JPG بالدقة العادية أو المضاعفة، وZIP بكل المقاسات، وسلسلة شرائح، ومشاريع، وقوالب.

## ٣. خريطة الملفات

| الملف | ما فيه |
|---|---|
| `index.html` | القشرة فقط: الأقسام، ونافذة القصّ، والاختصارات، وترتيب تحميل السكربتات |
| `style.css` | `@font-face` للخطوط، و**كل الألوان توكنات في `:root`**، ثم الواجهة |
| `data.js` | الجهات، والمواضيع، والأساليب، وبنك الأفكار `AW_IDEAS` (مصادر الأرقام في رأسه) |
| `js/util.js` | أدوات عامة، و`C('اسم')` لقراءة لون من توكنات CSS |
| `js/storage.js` | IndexedDB: `autosave` · `projects` · `series` · `templates` · `img:<id>` · `font:<id>` |
| `js/fonts.js` | مكتبة 16 خطاً محلياً، ورفع الخطوط |
| `js/kits.js` | المقاسات `FORMATS`، والهويات `KITS`، وثوابت العقاب وبدائل ألوانه |
| `js/doc.js` | **نموذج المستند**، والتخطيط التلقائي `autoLayout`، وتغيير المقاس، وترحيل الصيغة القديمة، والقوالب |
| `js/image.js` | معالجة الصورة بالبكسل، والفلاتر، ونافذة القصّ |
| `js/render.js` | `renderDoc`: رسم واحد للمعاينة والتصدير |
| `js/generator.js` | مولّد الأفكار والبرومبت |
| `js/editor.js` | الاختيار والسحب والمقابض والمغناطيس والمساطر والاختصارات |
| `js/export.js` | التصدير وZIP والسلسلة والمشاريع والقوالب والتحذيرات |
| `js/panels.js` | لوحات التحكم، ونظام ربط واحد (`data-t` الهدف و`data-k` المسار) |
| `js/app.js` | الحالة `S`، والتاريخ، و`commit()`، والتشغيل |
| `assets/eagle-data.js` · `assets/avatar-data.js` | العقاب الرسمي وأفاتار خلدون مضمّنان (base64) للرسم على اللوحة |
| `images/` | `eagle-emblem-metal.svg` للواجهة، و`og-card.jpg` بطاقة المشاركة |
| `fonts/` | خطوط woff2 مع رخص OFL |
| `DESIGN.md` | بطاقة الهوية والتوكنات، و`النوع: أداة` |

## ٤. كيف يعمل (اقرأه قبل أي تعديل)

- **التصميم مستند:** `S.doc = {fmt, kit, palette, bg, layout, scrim, entIdx, customEnt, caption, layers[]}`.
- **الطبقة:** `{id, type, role, x, y, w, h, visible, locked, opacity, manual, …}`، والإحداثيات ببكسل المقاس.
- **الأدوار الثابتة:** `photo · scrim · ornament · rule · eagle · entAr · entEn · head · sub · slogan · tag · handle · watermark · avatar · signature`. والمضافة: `free · logo · shape · eagleX`.
- **`autoLayout(doc)`** يضع كل طبقة ليست `manual`. ما يسحبه المستخدم يُعلَّم `manual` فيبقى في مكانه، و`sizeLock` تحفظ حجماً اختاره.
- **كل تعديل يمرّ من `commit(fn)`:** يطبّق، ثم يعيد التخطيط، ثم يسجّل في التاريخ، ثم يحفظ، ثم يرسم.
- **الأداء:** المعاينة تُرسم بـ`renderDoc` بمقياس أصغر، والتصدير بالمقياس الكامل، فالمعاينة هي التصدير.
- **لإضافة عنصر تحكم:** `sl()` أو `chips()` أو `colorCtl()` في `panels.js` مع `data-t`/`data-k`، ولا يُكتب مستمع خاص.
- **لإضافة خط:** ملف woff2 في `fonts/`، وسطر في `FONT_LIBRARY`، و`@font-face` في `style.css`.
- **لإضافة لون:** توكن في `:root` ثم `C('الاسم')` في JS. **ممنوع أي hex مكتوب في CSS خارج `:root` أو في JS.**

## ٥. قواعد لا تُكسر

1. **العقاب:**
   - الملف الرسمي وحده، ولا يُولَّد ولا يُعاد رسمه.
   - النسبة مقفلة 1000:754.1، والنجوم الثلاث جزء منه.
   - العرض لا يقل عن 48px، والشفافية لا تقل عن 30٪.
   - لا تدوير ولا قلب ولا ظل ولا توهّج ولا إطار.
   - الحيّز الآمن ربع عرضه.
   - الألوان ستة فقط: الذهبي الأصلي، وlight، وdark، وforest، وnavy_amber، وcream_navy.
   - هذه القيود مفروضة في `drawEagle` وفي لوحة التحكم، فلا تُرفع.
2. **هوية خلدون:**
   - التوقيع `@𝓚𝓱𝓪𝓵𝓭𝓸𝓾𝓷𝓐𝓴𝓻𝓪𝓶𝓪𝓱` بشفافية 12–18٪ فقط، ولونه من المشهد.
   - الأفاتار المعتمد بارتفاع 5–6٪ من التصميم، وشفافيته `0.22+0.10·L/255+0.10·min(sd/40,1)` محصورة بين 0.22 و0.45.
   - الاثنان وحدة إلزامية في هوية خلدون.
3. **لا رقم ولا حساب بلا مصدر:**
   - كل رقم في بنك الأفكار مصدره مكتوب في رأس `AW_IDEAS`.
   - حسابات الجهات الرسمية فارغة عمداً حتى يُتحقَّق منها على المنصّة.
4. **الجهات الرسمية:** علامة «هذا الإعلان غير حكومي» تُفعَّل تلقائياً عند اختيار جهة رسمية، ويبقى التحذير ظاهراً.
5. **الترقيم العربي:** لا `—` ولا `–` في أي نص يراه المستخدم. الحوار بين «»، والبدل إعادة صياغة لا حذف.
6. **الخطوط محلية:** لا Google Fonts، واللوحة تنتظر `document.fonts.load` قبل الرسم.
7. **العربية على اللوحة** تُرسم بـ`fillText` (لا html2canvas، فهي تكسر الحروف). والنص اللاتيني المختلط يُرسم في طبقته وباتجاهه، لأن `@حساب` داخل سطر عربي ينقلب.
8. **البيانات:** كل حفظ في IndexedDB. الصورة تُحفظ مرة بمعرّفها ولا تدخل اللقطات، ولا يُستعمل localStorage لبيانات المستخدم.

## ٦. التشغيل والفحص قبل أي تسليم

```bash
cd ~/khaldoun-library/Projects/awareness-studio
python3 -m http.server 8765
```

- **المتصفح:** افتح `http://localhost:8765`. وافتحه دائماً عبر خادم لا بنقر الملف مباشرة.
- **صحة الكود:** `node --check js/*.js`.
- **التجربة الفعلية:**
  - توليد فكرة.
  - رفع صورة 16:9 وأخرى فيها كتابة، وتجربة «احتواء كامل».
  - سحب عنصر ثم التراجع.
  - تصدير «كل المنصات» وفتح الـZIP.
  - عرض 375px بلا امتداد أفقي.
  - كونسول بلا أخطاء.
- **بوابة الويب:** `bash ~/khaldoun-library/wiki/_tools/بوابة-الويب.sh` يجب أن تنتهي بـ«اجتازت».
- **بعد تعديل أي ملف CSS أو JS:** ارفع رقم `?v=` في روابط `index.html` كي يصل التحديث للمستخدمين.

## ٧. النشر

- **الموافقة:** لا يُنشر شيء إلا بموافقة خلدون الصريحة.
- **الترتيب:** بوابة الويب، ثم فحوص سكيل `khaldoun-nashr`، ثم `git push` إلى `main`، ثم التحقق من الرابط الحيّ.
- **رسالة الـcommit:** بصيغة `<الفعل>: <الوصف>`.
- **البوابة الرئيسية:** إن تغيّر وصف الأداة، تُحدَّث بطاقتها في `Kakramah/Kakramah.github.io`: الوصف في `projects.json`، والمصغّرة `images/thumb-awareness-studio.webp` بمقاس 640×360، ثم `python3 build.py`. ولا يُعدَّل `index.html` هناك باليد.

## ٨. قالب الطلب

انسخه وعدّل آخره:

> أعمل على «استوديو التوعية»، أداة ويب عربية بلا خادم في المستودع Kakramah/awareness-studio. اقرأ `BRIEF.md` ثم الملفات التي يخصّها طلبي كاملةً قبل أي تعديل، والتزم بالقسم ٥ حرفياً. خذ نسخة احتياطية قبل التعديل، وجرّب التغيير فعلياً في المتصفح، ثم شغّل بوابة الويب. لا تنشر إلا بعد موافقتي.
>
> **طلبي:** …
