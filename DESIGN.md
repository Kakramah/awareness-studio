# DESIGN · استوديو التوعية

النوع: أداة

## بطاقة الهوية (10.3)

| الحقل | القيمة | السبب |
|---|---|---|
| المكان | سوريا كلها، بلا مدينة بعينها | الأداة تصنع منشورات لأي محافظة أو مبادرة |
| الزمان | 2026، نهار العمل | أداة إنتاج يومية، فالواجهة فاتحة هادئة واللوحة وحدها تحمل اللون |
| الطبيعة | دعوة مدنية وأداة إنتاج | تخدم التوعية العامة، ولا تحكي قصة واحدة |
| الشعور المهيمن | الثقة والتمكّن | المستخدم يجب أن يشعر أن كل عنصر تحت يده |
| الأسلوب | هوية سوريا الجديدة (سيادية مفروضة، 10.4) | الأداة وُلدت منها، ولها هويتان أخريان داخل المحرّر: خلدون والحرّ |
| العائلة الخطية | Cairo للعناوين، Tajawal للمتن، El Messiri لشارة الشعار | من مكتبة 3.6.6؛ والمحرّر يحمل 16 خطاً محلياً للمستخدم |
| معالجة العقاب | `gold` (الأصل) في الواجهة والبوستر افتراضياً، 150px عرضاً على 1080، شفافية 100٪؛ والبدائل الخمسة داخل المحرّر | قواعد canonical-values §العقاب مفروضة في الكود: نسبة مقفلة، ≥48px، شفافية ≥30٪، لا تدوير ولا ظل |

## التوكنات

كل لون في `:root` داخل `style.css`، ولا لون حرّ في CSS أو JS. الكود يقرأ اللون بـ `C('الاسم')` من `js/util.js`.

- **الواجهة:** `--green` `--pine` `--gold` `--goldSoft` `--cream` `--creamDeep` `--sage` `--danger` `--ink` وتدرّجات `--muted*` و`--text-*`.
- **الهويات:** `--<هوية>-<باليتة>-<دور>`، مثل `--syria-dark-head` و`--khaldoun-light-bg1`، ومعها `--<هوية>-sw1..8` لعيّنات الألوان.
- **العقاب:** `--eagle-light` `--eagle-dark` `--eagle-forest` `--eagle-navy-amber` `--eagle-cream-navy`، وهي بدائل `SCHEMES` في `poster-for-jableh/scripts/compose.py`.

## الخط

24 ملف woff2 في `fonts/` مع رخص OFL، معرّفة بـ `@font-face` في `style.css` مع `font-display: swap`. اللوحة تنتظر `document.fonts.load` قبل الرسم، فلا يُصدَّر بوستر بخط بديل.

## الشبكة

واجهة من ثلاثة أعمدة: شريط أقسام 66px، ومسرح مرن، ولوحة خصائص 360px. تحت 900px تتكدّس عموداً واحداً، وتحت 600px تختفي التسميات الثانوية.

## الحركة

انتقالات قصيرة 0.15s على الأزرار وحدها، وكلها تُطفأ داخل `prefers-reduced-motion: reduce`.

## جرد البنوك

- `data.js`: 18 جهة (بلا حسابات حتى يُتحقَّق منها)، 16 موضوعاً، 8 أساليب بلاغية، 90 فكرة، 8 وصفات بصرية. مصادر كل رقم في رأس `AW_IDEAS`.
- `assets/eagle-data.js`: العقاب الرسمي (بصمة `2e2ac159…`) للرسم على اللوحة.
- `assets/avatar-data.js`: أفاتار خلدون المعتمد (بصمة `2134e128…`).
- `images/eagle-emblem-metal.svg`: العقاب الرسمي للواجهة.
- `images/og-card.jpg`: بطاقة المشاركة 1200×630، مرسومة بمحرّك الاستوديو نفسه.
